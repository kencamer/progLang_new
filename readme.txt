The user gzip is the database
Import it first to the xampp sql
Then run everything else on vscode as the folder
Call the portfolio first because it contains the button to the login form then to the dashboard for crud
That is all. Take a breather and have a nice day!
